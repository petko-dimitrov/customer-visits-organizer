{% extends 'base.html.twig' %}

{% block main %}
    <div class="container body-content span=8 offset=2">
        <div class="well">
            <form class="form-horizontal" action="{{ path('add_contact', {id: id}) }}" method="post">
                <fieldset>
                    <legend>Add Contact for {{ customer.name }}</legend>
                    {% for error in errors %}
                        <div class="alert alert-danger">
                            <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                            {{ error.message }}
                        </div>
                    {% endfor %}
                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="name">Name</label>
                        <div class="col-sm-4 ">
                            <input class="form-control" id="name" placeholder="Name" name="contact[name]"
                                   required type="text">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="phone">Phone Number</label>
                        <div class="col-sm-4 ">
                            <input class="form-control" id="phone" placeholder="Phone Number" name="contact[phone]"
                                   required type="text" value="{{ customer.phone }}">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="email">Email</label>
                        <div class="col-sm-4 ">
                            <input class="form-control" id="email" placeholder="Email" name="contact[email]"
                                   type="email" value="{{ customer.email }}">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="name">Job Title</label>
                        <div class="col-sm-4 ">
                            <input class="form-control" id="jobTitle" placeholder="Job Title" name="contact[jobTitle]"
                                   type="text">
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-sm-4 col-sm-offset-4">
                            <a class="btn btn-default" href="{{ path('all_customers') }}">Cancel</a>
                            <button type="submit" class="btn btn-primary">Add Contact</button>
                        </div>
                    </div>

                    {{ form_row(form._token) }}
                </fieldset>
            </form>
        </div>
    </div>
{% endblock %}

